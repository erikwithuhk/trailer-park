const User = require('../models/User');
const Trailer = require('../models/Trailer');

class UserTrailer {
  // constructor({ user_id, email, username, first_name, last_name, bio, password, trailers.tmdb_id, title, media_type, blocked }) {
  //   this.user = user;
  //   this.trailer = trailer;
  // }
}

module.exports = UserTrailer;
